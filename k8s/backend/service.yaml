apiVersion: v1
kind: Service
metadata:
  name: expenseiq-backend-service
  namespace: expenseiq
  labels:
    app: expenseiq-backend
    app.kubernetes.io/part-of: expenseiq
spec:
  selector:
    app: expenseiq-backend
  ports:
    - port: 3000
      targetPort: 3000
      name: http
